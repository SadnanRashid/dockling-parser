


parser.py: Main unedited file.
parser_.py: Edit with the fix.


Needs fixing:
1/ Same content is parsed as table and image at the same time. Avoid this duplication.
2/ Some tables are not being parsed. Parsing as image.